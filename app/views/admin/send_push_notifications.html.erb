<h3>Send push notification</h3>
<div>
  <span style="font-weight:bold;color:red">Use with caution:</span>
  This is a DB and memory intensive process and might block the live instance.
</div><br/>

<%= render 'shared/flash_notices' %>

<%= form_tag send_push_notifications_admin_path, method: 'post', class: 'form' do %>
  <div class="control-group">
    <label>Message (120 chars max):</label>
    <input type="text" name="message" class="span8" />
  </div>

  <input type="submit" class="btn btn-primary" value="Send" />
<% end %>
